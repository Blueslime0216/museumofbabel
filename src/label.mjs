// 벽 라벨 — 코드워드에서 작품 제목과 소장품 번호를 만든다
//
// 요구사항 7장.
//   좌표로 생성된 그림에는 설명할 내용이 원래 없다. 기술 수치만 늘어놓으면
//   미술관이 아니라 개발자 도구가 된다. 그래서 제목을 만든다.
//
//   저장소도 서버도 없다. 같은 좌표는 언제나 같은 제목이 된다.
//   색 이름은 실제 팔레트 필드에서 뽑으므로 **제목의 색이 그림의 색과 맞는다.**
//
// 언어마다 단어를 따로 짓는다. 직역하면 어색하다.
//
// ── 문법을 조건문으로 맞추지 않는다 ─────────────────────────────────────
//
// 낱말을 틀에 끼우는 방식이라 언어마다 걸리는 곳이 다르다. 걸릴 때마다 예외를
// 두는 대신 **애초에 걸리지 않는 틀과 낱말만 쓴다.**
//
//   한국어   조사를 쓰지 않는다. "{A}과 {B}" 는 받침에 따라 과/와가 갈린다.
//            가운뎃점과 쉼표로만 잇는다.
//   러시아어 명사를 모두 남성으로 골랐다. 형용사가 성에 따라 어미를 바꾸므로
//            섞으면 "Тихий простор" 와 "Тихая ниша" 를 가려 써야 한다.
//            색 이름은 격 변화를 피하려고 콜론 · 줄표 · 괄호 뒤에 세운다.
//   일본어   형용사를 모두 연체형으로 적었다. 그대로 명사에 붙는다.
//   중국어   형용사에 "的" 를 붙여 두었다. 어느 명사에나 붙는다.
//
// 표의 크기는 언어마다 같아야 한다 (색 12 · 무채색 8 · 형용사 32 · 명사 32 ·
// 형식 8). 코드워드의 자리에서 뽑는 번호가 그 크기를 전제하기 때문이다.
// 하나라도 어긋나면 같은 좌표가 언어에 따라 다른 자리에서 나온다.
// `test/label.test.mjs` 가 크기를 직접 센다.

import { tierSpec, decodeFields, toBase36, roomOf, ROOMS, isLobbyTier } from './codec.mjs';
import { language } from './i18n/index.mjs';

/** Cb·Cr 각도에 대응하는 색 이름. 0도가 파랑, 반시계로 돈다. 열두 개. */
const HUES = {
  en: [
    'Indigo', 'Violet', 'Mauve', 'Carmine', 'Crimson', 'Rust',
    'Amber', 'Ochre', 'Olive', 'Verdigris', 'Celadon', 'Teal',
  ],
  ko: [
    '쪽빛', '보라', '연자주', '연지', '다홍', '녹슨빛',
    '호박빛', '황토', '올리브', '녹청', '청자빛', '물빛',
  ],
  ja: [
    '藍', '菫色', '藤色', '臙脂', '緋色', '錆色',
    '琥珀', '黄土', '苔色', '緑青', '青磁', '浅葱',
  ],
  zh: [
    '靛蓝', '紫罗兰', '藕紫', '胭脂', '绛红', '铁锈',
    '琥珀', '赭石', '橄榄', '铜绿', '青瓷', '水鸭青',
  ],
  ru: [
    'Индиго', 'Аметист', 'Мальва', 'Кармин', 'Багрец', 'Ржавчина',
    // «Медянка» без «ярь-» — это змея. Пигмент называется «ярь-медянка».
    'Янтарь', 'Охра', 'Олива', 'Ярь-медянка', 'Селадон', 'Бирюза',
  ],
};

/** 채도가 낮을 때 쓰는 무채색 이름. 어두운 것부터 여덟 개. */
const NEUTRALS = {
  en: ['Soot', 'Char', 'Slate', 'Pewter', 'Ash', 'Bone', 'Ivory', 'Chalk'],
  ko: ['그믐', '숯', '청회', '백랍', '잿빛', '상아', '백지', '서리'],
  // 「生成り」の送り仮名を落とすと「せいせい」と読まれる。
  ja: ['煤色', '墨', '青鈍', '錫色', '灰', '生成り', '象牙', '白磁'],
  zh: ['煤黑', '焦墨', '石板灰', '锡白', '烟灰', '骨白', '象牙', '白垩'],
  ru: ['Сажа', 'Уголь', 'Сланец', 'Олово', 'Пепел', 'Кость', 'Слоновая кость', 'Мел'],
};

const ADJECTIVES = {
  en: [
    'Quiet', 'Broken', 'Folded', 'Adjacent', 'Distant', 'Recurring', 'Vertical', 'Horizontal',
    'Interrupted', 'Standing', 'Falling', 'Doubled', 'Hollow', 'Dense', 'Shallow', 'Late',
    'Early', 'Unfinished', 'Provisional', 'Persistent', 'Silent', 'Divided', 'Nested', 'Reversed',
    'Slanted', 'Suspended', 'Wandering', 'Fixed', 'Vacant', 'Crowded', 'Narrow', 'Endless',
    'Luminous', 'Submerged', 'Rotating', 'Cracked', 'Woven', 'Scattered', 'Frozen', 'Drifting',
    'Buried', 'Ascending', 'Descending', 'Mirrored', 'Braided', 'Bleached', 'Charred', 'Sealed',
    'Tilted', 'Compressed', 'Faded', 'Layered', 'Perforated', 'Smouldering', 'Veiled', 'Rusted',
    'Sunken', 'Returning', 'Departed', 'Awaiting', 'Trembling', 'Weathered', 'Hidden', 'Patient',
  ],
  ko: [
    '고요한', '부서진', '접힌', '맞닿은', '먼', '되풀이되는', '곧은', '누운',
    '끊긴', '선', '떨어지는', '겹친', '빈', '촘촘한', '얕은', '늦은',
    '이른', '미완의', '임시의', '끈질긴', '말없는', '갈라진', '품은', '뒤집힌',
    '기운', '매달린', '떠도는', '붙박인', '지워진', '붐비는', '좁은', '끝없는',
    // 22번째에 이미 '갈라진'(Divided) 이 있으므로 Cracked 는 '터진' 으로 갈랐다.
    '빛나는', '잠긴', '도는', '터진', '짜인', '흩어진', '얼어붙은', '흐르는',
    '묻힌', '오르는', '내리는', '비친', '엮인', '바랜', '그을린', '닫힌',
    '비스듬한', '눌린', '흐려진', '겹겹의', '뚫린', '타는', '가린', '녹슨',
    '가라앉은', '돌아오는', '떠난', '기다리는', '떨리는', '삭은', '숨은', '참는',
  ],
  // 연체形。そのまま名詞に付く。
  ja: [
    '静かな', '壊れた', '折れた', '隣り合う', '遠い', '繰り返す', '垂直の', '水平の',
    '途切れた', '立つ', '落ちる', '重なる', '空ろな', '密な', '浅い', '遅い',
    '早い', '未完の', '仮の', '執拗な', '無言の', '割れた', '内包する', '裏返しの',
    '傾いた', '吊られた', 'さまよう', '固定の', '消えた', '混み合う', '狭い', '果てしない',
    '光る', '沈んだ', '回る', '裂けた', '織られた', '散った', '凍った', '流れる',
    '埋もれた', '昇る', '降る', '映った', '編まれた', '褪せた', '焦げた', '閉じた',
    '斜めの', '押された', '曇った', '幾重の', '穿たれた', '燻る', '覆われた', '錆びた',
    '沈没の', '戻る', '去った', '待つ', '震える', '風化した', '隠れた', '耐える',
  ],
  // 都带「的」，接哪个名词都成。
  // 单音节加「的」读起来薄（「浅的野」），所以都用双音节。
  zh: [
    '安静的', '破碎的', '折叠的', '相邻的', '遥远的', '重复的', '垂直的', '水平的',
    '断裂的', '站立的', '坠落的', '重叠的', '中空的', '稠密的', '浅浅的', '迟来的',
    '早来的', '未完的', '临时的', '执拗的', '沉默的', '分开的', '内嵌的', '翻转的',
    '倾斜的', '悬挂的', '游荡的', '固定的', '空置的', '拥挤的', '狭窄的', '无尽的',
    '明亮的', '沉没的', '旋转的', '裂开的', '编织的', '散落的', '冰封的', '流动的',
    '掩埋的', '上升的', '下降的', '映照的', '缠绕的', '褪色的', '焦黑的', '封闭的',
    '斜倾的', '压缩的', '模糊的', '层叠的', '穿孔的', '阴燃的', '遮蔽的', '生锈的',
    '下沉的', '归来的', '离去的', '等待的', '颤动的', '风化的', '隐藏的', '忍耐的',
  ],
  // Мужской род, именительный падеж. Существительные ниже — тоже мужского рода.
  ru: [
    'Тихий', 'Разбитый', 'Сложенный', 'Смежный', 'Далёкий', 'Повторяющийся', 'Вертикальный', 'Горизонтальный',
    'Прерванный', 'Стоящий', 'Падающий', 'Удвоенный', 'Полый', 'Плотный', 'Неглубокий', 'Поздний',
    'Ранний', 'Незаконченный', 'Временный', 'Упорный', 'Безмолвный', 'Разделённый', 'Вложенный', 'Обращённый',
    'Наклонный', 'Подвешенный', 'Блуждающий', 'Закреплённый', 'Пустой', 'Тесный', 'Узкий', 'Бесконечный',
    'Светящийся', 'Затопленный', 'Вращающийся', 'Треснувший', 'Плетёный', 'Рассыпанный', 'Замёрзший', 'Дрейфующий',
    'Погребённый', 'Восходящий', 'Нисходящий', 'Отражённый', 'Заплетённый', 'Выцветший', 'Обожжённый', 'Запечатанный',
    'Скошенный', 'Сжатый', 'Померкший', 'Многослойный', 'Пробитый', 'Тлеющий', 'Укрытый', 'Заржавленный',
    'Затонувший', 'Возвращающийся', 'Ушедший', 'Ждущий', 'Дрожащий', 'Выветренный', 'Скрытый', 'Терпеливый',
  ],
};

const NOUNS = {
  en: [
    'Field', 'Passage', 'Interval', 'Terrace', 'Threshold', 'Aperture', 'Register', 'Cadence',
    'Partition', 'Lattice', 'Corridor', 'Fragment', 'Margin', 'Enclosure', 'Meridian', 'Sequence',
    'Chamber', 'Vestibule', 'Plateau', 'Ledger', 'Facade', 'Column', 'Archive', 'Cornice',
    'Alcove', 'Cistern', 'Rampart', 'Portico', 'Palisade', 'Reliquary', 'Annex', 'Rotunda',
    'Aqueduct', 'Belfry', 'Cloister', 'Conduit', 'Cupola', 'Dovecote', 'Embankment', 'Foundry',
    'Granary', 'Hearth', 'Keystone', 'Lantern', 'Lintel', 'Mezzanine', 'Obelisk', 'Parapet',
    'Pediment', 'Pilaster', 'Quarry', 'Refectory', 'Scaffold', 'Spandrel', 'Stairwell', 'Transept',
    'Trellis', 'Undercroft', 'Vault', 'Verandah', 'Watchtower', 'Weir', 'Wellhead', 'Zenith',
  ],
  ko: [
    '들', '통로', '사이', '단', '문턱', '틈', '층계', '가락',
    '칸막이', '격자', '복도', '조각', '여백', '울', '자오선', '차례',
    '방', '현관', '고원', '대장', '벽면', '기둥', '서고', '처마',
    '벽감', '저수조', '성벽', '주랑', '울짱', '성물함', '별관', '원형실',
    '수로', '종루', '회랑', '도관', '돔', '비둘기집', '둑', '주철소',
    '곡물창고', '화덕', '쐐기돌', '등롱', '상인방', '중층', '오벨리스크', '흉벽',
    '박공', '벽기둥', '채석장', '식당간', '비계', '아치옆면', '계단실', '익랑',
    '격자울', '지하실', '궁륭', '툇마루', '망루', '어살', '우물머리', '천정점',
  ],
  // 「閾」は閾値で見る字で、敷居の意味では読みにくい。
  // 「水槽」は今の日本語では魚を飼う器なので「貯水槽」にした。
  ja: [
    '野', '通路', '間', '段', '敷居', '隙', '層', '調べ',
    '仕切り', '格子', '廊', '断片', '余白', '囲い', '子午線', '序列',
    '広間', '玄関', '台地', '台帳', '壁面', '柱', '書庫', '軒',
    '壁龕', '貯水槽', '城壁', '柱廊', '柵', '聖遺物箱', '別館', '円堂',
    '水路', '鐘楼', '回廊', '導管', '円蓋', '鳩舎', '堤', '鋳造所',
    '穀倉', '炉', '楔石', '灯籠', '楣', '中二階', '尖塔柱', '胸壁',
    '破風', '付柱', '石切場', '食堂間', '足場', '三角壁', '階段室', '翼廊',
    '棚格子', '地下室', '穹窿', '縁側', '望楼', '簗', '井筒', '天頂',
  ],
  // 单字站不住的都补成双音节（野→原野、台→露台、缝→缝隙、韵→韵律、檐→檐口）。
  // 「层」在这个应用里是楼层，所以 Register 换成「音域」，免得两个意思撞上。
  zh: [
    '原野', '通道', '间隔', '露台', '门槛', '缝隙', '音域', '韵律',
    '隔断', '格栅', '走廊', '碎片', '留白', '围栏', '子午线', '序列',
    '内室', '前厅', '高台', '账册', '立面', '柱', '书库', '檐口',
    '壁龛', '水窖', '城墙', '柱廊', '木栅', '圣物匣', '别馆', '圆厅',
    '水道', '钟楼', '回廊', '导管', '穹顶', '鸽舍', '堤岸', '铸场',
    '谷仓', '炉膛', '拱心石', '灯笼', '门楣', '夹层', '方尖碑', '胸墙',
    '山花', '壁柱', '采石场', '食堂', '脚手架', '拱肩', '楼梯井', '耳堂',
    '花架', '地窨', '穹窿', '廊台', '望楼', '鱼梁', '井口', '天顶',
  ],
  // Все — мужского рода. Иначе прилагательные выше пришлось бы согласовывать.
  // «Покой» в единственном числе читается как «спокойствие», поэтому «Чертог».
  // «Водоём» — это водная гладь, а не цистерна: «Резервуар».
  ru: [
    'Простор', 'Проход', 'Интервал', 'Уступ', 'Порог', 'Просвет', 'Регистр', 'Ритм',
    'Простенок', 'Переплёт', 'Коридор', 'Фрагмент', 'Отступ', 'Двор', 'Меридиан', 'Ряд',
    'Чертог', 'Вестибюль', 'Ярус', 'Реестр', 'Фасад', 'Столб', 'Архив', 'Карниз',
    'Альков', 'Резервуар', 'Вал', 'Портик', 'Частокол', 'Ковчег', 'Флигель', 'Зал',
    // Тоже все мужского рода. Женские («колокольня», «насыпь») заменены.
    'Акведук', 'Клуатр', 'Водовод', 'Купол', 'Голубятник', 'Откос', 'Горн', 'Амбар',
    'Очаг', 'Замок', 'Фонарь', 'Брус', 'Полуярус', 'Обелиск', 'Парапет', 'Фронтон',
    'Пилястр', 'Карьер', 'Рефекторий', 'Помост', 'Распалубок', 'Пролёт', 'Трансепт', 'Шпалерник',
    'Подклет', 'Свод', 'Навес', 'Донжон', 'Затвор', 'Колодец', 'Зенит', 'Маяк',
  ],
};

/**
 * 제목 형식 여덟 가지.
 *
 * 한국어는 조사를 쓰지 않는다. 단어를 넣는 순간 받침에 따라 문법이 깨지기 때문이다.
 * 가운뎃점과 쉼표만으로 잇는다. 그래도 현대미술 제목처럼 읽힌다.
 */
const FORMS = {
  en: [
    ({ a, b }) => (b ? `Composition in ${a} and ${b}` : `Composition in ${a}`),
    ({ a }) => `Study in ${a}`,
    ({ adjective, noun }) => `${adjective} ${noun}`,
    ({ noun, a }) => `${noun} with ${a}`,
    ({ adjective, noun }) => `Untitled (${adjective} ${noun})`,
    ({ a, noun }) => `${a} ${noun}`,
    ({ a }) => `Arrangement in ${a}`,
    ({ noun, a, b, adjective }) => (b ? `${noun} in ${a} and ${b}` : `${adjective} ${noun}`),
    ({ noun, a }) => `${noun}: ${a}`,
    ({ adjective, a }) => `${adjective} ${a}`,
    ({ noun }) => `Notes on the ${noun}`,
    ({ a, b }) => (b ? `Between ${a} and ${b}` : `Toward ${a}`),
    ({ adjective, noun }) => `${noun}, ${adjective}`,
    ({ a, noun }) => `Interior with ${a} ${noun}`,
    ({ noun, a }) => `After the ${noun} (${a})`,
    ({ adjective, noun, a }) => `Study of a ${adjective} ${noun} in ${a}`,
  ],
  ko: [
    ({ a, b }) => (b ? `${a} · ${b} 구성` : `${a} 구성`),
    ({ a }) => `${a} 습작`,
    ({ adjective, noun }) => `${adjective} ${noun}`,
    ({ noun, a }) => `${noun}, ${a}`,
    ({ adjective, noun }) => `무제 (${adjective} ${noun})`,
    ({ a, noun }) => `${a} ${noun}`,
    ({ a }) => `${a} 배치`,
    ({ noun, a, b, adjective }) => (b ? `${noun} · ${a} · ${b}` : `${adjective} ${noun}`),
    // 조사를 쓰지 않는 규칙은 여기서도 지킨다. "와/과" 는 받침에 따라 갈리므로
    // 두 색을 이을 때도 가운뎃점만 쓴다. "에" 와 "쪽으로" 는 받침과 무관하다.
    ({ noun, a }) => `${noun}: ${a}`,
    ({ adjective, a }) => `${adjective} ${a}`,
    ({ noun }) => `${noun}에 관한 노트`,
    ({ a, b }) => (b ? `${a} · ${b} 사이` : `${a} 쪽으로`),
    ({ adjective, noun }) => `${noun}, ${adjective}`,
    ({ a, noun }) => `${a} ${noun} 실내`,
    ({ noun, a }) => `${noun} 이후 (${a})`,
    ({ adjective, noun, a }) => `${adjective} ${noun} 연구 · ${a}`,
  ],
  ja: [
    ({ a, b }) => (b ? `${a}と${b}の構成` : `${a}の構成`),
    ({ a }) => `${a}の習作`,
    ({ adjective, noun }) => `${adjective}${noun}`,
    ({ noun, a }) => `${noun}、${a}`,
    ({ adjective, noun }) => `無題（${adjective}${noun}）`,
    ({ a, noun }) => `${a}の${noun}`,
    ({ a }) => `${a}の配置`,
    ({ noun, a, b, adjective }) => (b ? `${noun}・${a}・${b}` : `${adjective}${noun}`),
    ({ noun, a }) => `${noun}：${a}`,
    ({ adjective, a }) => `${adjective}${a}`,
    ({ noun }) => `${noun}についての覚書`,
    ({ a, b }) => (b ? `${a}と${b}の間` : `${a}へ`),
    ({ adjective, noun }) => `${noun}、${adjective}`,
    ({ a, noun }) => `${a}の${noun}のある室内`,
    ({ noun, a }) => `${noun}のあと（${a}）`,
    ({ adjective, noun, a }) => `${adjective}${noun}の研究・${a}`,
  ],
  zh: [
    ({ a, b }) => (b ? `${a}与${b}的构成` : `${a}的构成`),
    ({ a }) => `${a}习作`,
    ({ adjective, noun }) => `${adjective}${noun}`,
    ({ noun, a }) => `${noun}，${a}`,
    ({ adjective, noun }) => `无题（${adjective}${noun}）`,
    ({ a, noun }) => `${a}${noun}`,
    ({ a }) => `${a}的布置`,
    // 가운뎃점은 U+00B7 이다. 일본어의 U+30FB 를 쓰면 가나 블록에 들어간다.
    ({ noun, a, b, adjective }) => (b ? `${noun}·${a}·${b}` : `${adjective}${noun}`),
    ({ noun, a }) => `${noun}：${a}`,
    ({ adjective, a }) => `${adjective}${a}`,
    ({ noun }) => `关于${noun}的笔记`,
    ({ a, b }) => (b ? `${a}与${b}之间` : `向${a}`),
    ({ adjective, noun }) => `${noun}，${adjective}`,
    ({ a, noun }) => `有${a}${noun}的室内`,
    ({ noun, a }) => `${noun}之后（${a}）`,
    ({ adjective, noun, a }) => `${adjective}${noun}研究·${a}`,
  ],
  // 낱말은 대문자로 저장해 두었다. 시트의 "색" 칸에 홀로 설 때 그것이 맞다.
  // 제목 안에서는 첫 낱말만 대문자로 남기는 것이 러시아어 관례이므로 여기서
  // 내려 쓴다. 색 이름은 콜론 · 줄표 · 괄호 뒤에 세워 격 변화를 피한다.
  ru: [
    ({ a, b }) => (b ? `Композиция: ${low(a)} и ${low(b)}` : `Композиция: ${low(a)}`),
    ({ a }) => `Этюд: ${low(a)}`,
    ({ adjective, noun }) => `${adjective} ${low(noun)}`,
    ({ noun, a }) => `${noun} — ${low(a)}`,
    ({ adjective, noun }) => `Без названия (${low(adjective)} ${low(noun)})`,
    ({ a, noun }) => `${noun} (${low(a)})`,
    ({ a }) => `Расположение: ${low(a)}`,
    ({ noun, a, b, adjective }) =>
      b ? `${noun} · ${low(a)} · ${low(b)}` : `${adjective} ${low(noun)}`,
    // 전치사를 쓰면 격을 맞춰야 한다. «Между» 는 조격, «К» 는 여격을 요구하므로
    // 낱말을 그대로 끼울 수 없다. 콜론 · 줄표 · 괄호로만 잇는 규칙을 여기서도 지킨다.
    ({ noun, a }) => `${noun}: ${low(a)}`,
    ({ adjective, a }) => `${adjective} — ${low(a)}`,
    ({ noun }) => `Заметки: ${low(noun)}`,
    ({ a, b }) => (b ? `Границы: ${low(a)}, ${low(b)}` : `Предел: ${low(a)}`),
    ({ adjective, noun }) => `${noun}, ${low(adjective)}`,
    ({ a, noun }) => `Интерьер: ${low(noun)} (${low(a)})`,
    ({ noun, a }) => `После: ${low(noun)} — ${low(a)}`,
    ({ adjective, noun, a }) => `Исследование: ${low(adjective)} ${low(noun)}, ${low(a)}`,
  ],
};

/**
 * 명작 — 조합이 아니라 고유 제목이 붙는 작품.
 *
 * ── 왜 두는가 ────────────────────────────────────────────────────────────
 *
 * 낱말을 끼우는 제목은 아무리 늘려도 "틀에서 나온 것" 으로 읽힌다. 실제 미술관에는
 * 번호가 아니라 **이름으로 불리는 작품**이 몇 점 있고, 그것이 그 미술관의 기억이
 * 된다. 여기서도 아주 드물게 고유 제목이 나오게 했다.
 *
 * 확률은 1/512 이다. 한 화면에 100장쯤 걸리므로 몇 화면을 돌면 한 점을 만난다.
 * 흔하면 특별하지 않고, 너무 드물면 아무도 못 본다.
 *
 * 좌표에서 나오므로 **어느 명작이 어디 걸려 있는지는 처음부터 정해져 있다.**
 * 누가 먼저 찾아도 같은 자리다. 그래서 "찾았다" 를 남에게 링크로 보낼 수 있다.
 *
 * 낱말은 이 미술관 자신에 관한 것으로 골랐다 — 목록 · 사서 · 좌표 · 봉인된 별관.
 * 무한한 서고를 다룬 이야기의 결을 빌렸다.
 */
const MASTERWORKS = {
  en: [
    "The Cartographer's Error", 'Room Without a Door', 'The Last Catalogue',
    'Hexagon at Dusk', "The Copyist's Hand", 'Inventory of Absent Things',
    'The Sealed Wing', 'Portrait of a Coordinate', 'The Unread Shelf',
    'Mirror Facing a Mirror', "The Custodian's Lamp", 'What the Wall Remembers',
    'The Second Silence', 'Passage Found Twice', 'The Index of Colours',
    'Everything Already Hung',
  ],
  ko: [
    '지도 제작자의 오류', '문 없는 방', '마지막 목록',
    '해질녘의 육각형', '필경사의 손', '부재의 목록',
    '봉인된 별관', '좌표의 초상', '읽히지 않은 서가',
    '거울을 마주한 거울', '관리인의 등불', '벽이 기억하는 것',
    '두 번째 침묵', '두 번 찾은 통로', '색의 색인',
    '이미 걸려 있는 모든 것',
  ],
  ja: [
    '地図作者の誤り', '扉のない部屋', '最後の目録',
    '黄昏の六角形', '写字生の手', '不在のものの一覧',
    '封じられた別館', '座標の肖像', '読まれぬ棚',
    '鏡に向かう鏡', '管理人の灯', '壁が覚えていること',
    '二度目の沈黙', '二度見つけた通路', '色の索引',
    'すでに掛かっている全て',
  ],
  zh: [
    '制图者的错误', '没有门的房间', '最后的目录',
    '黄昏的六角形', '抄写者的手', '不在之物的清单',
    '封闭的别馆', '坐标的肖像', '未读的书架',
    '面向镜子的镜子', '看守人的灯', '墙所记得的',
    '第二次沉默', '两次找到的通道', '颜色的索引',
    '已经挂着的一切',
  ],
  ru: [
    'Ошибка картографа', 'Комната без двери', 'Последний каталог',
    'Шестиугольник в сумерках', 'Рука переписчика', 'Список отсутствующих вещей',
    'Запечатанный флигель', 'Портрет координаты', 'Непрочитанная полка',
    'Зеркало против зеркала', 'Лампа хранителя', 'Что помнит стена',
    'Второе молчание', 'Проход, найденный дважды', 'Указатель цветов',
    'Всё уже развешано',
  ],
};

/**
 * 연작 번호. 제목 뒤에 붙는다.
 *
 * 실제 미술 작품에 흔한 표기이고, 낱말 표를 늘리지 않고도 다양성이 커진다.
 * 로마 숫자는 어느 언어의 도록에서나 그대로 쓰므로 번역하지 않는다.
 * I 은 넣지 않았다 — 홀로 있는 작품에 "제1번" 을 붙이면 없는 연작을 암시한다.
 */
const SERIES = ['II', 'III', 'IV', 'V', 'VI', 'VII', 'VIII', 'IX'];

/** 첫 글자를 내려 쓴다. 러시아어 제목 안에서 쓴다. */
function low(word) {
  return word.charAt(0).toLowerCase() + word.slice(1);
}

/**
 * 제목 낱말 표. 검사가 크기와 낱말을 직접 본다 (`test/label.test.mjs`).
 *
 * 앱은 이것을 쓰지 않는다. 내보내는 이유는 하나다. 표의 크기가 어긋나면
 * 언어에 따라 제목이 다른 자리에서 나오는데, 좌표를 뽑아 보는 방식으로는
 * 32개 중 하나가 빈 것을 놓치기 쉽다. 세어 보는 편이 확실하다.
 */
export const TITLE_WORDS = {
  hues: HUES,
  neutrals: NEUTRALS,
  adjectives: ADJECTIVES,
  nouns: NOUNS,
  forms: FORMS,
  masterworks: MASTERWORKS,
};

/** 연작 번호. 언어를 가리지 않는다. 검사가 개수를 센다. */
export const TITLE_SERIES = SERIES;

/** 사전에 없는 언어는 영어로 떨어진다. 조용히 비지 않는다. */
function tableFor(source, lang) {
  return source[lang] ?? source.en;
}

/**
 * 크로마 한 쌍을 색 이름으로.
 *
 * cb 가 크면 파랑, cr 가 크면 빨강 쪽이다. 중앙에서 멀지 않으면 무채색으로 본다.
 */
function colorName(cb, cr, center, luma01, lang) {
  const neutrals = tableFor(NEUTRALS, lang);
  const hues = tableFor(HUES, lang);

  const db = cb - center;
  const dr = cr - center;
  if (Math.hypot(db, dr) < center * 0.22) {
    return neutrals[Math.min(neutrals.length - 1, Math.floor(luma01 * neutrals.length))];
  }
  let angle = Math.atan2(dr, db) / (Math.PI * 2);
  if (angle < 0) angle += 1;
  return hues[Math.floor(angle * hues.length) % hues.length];
}

/**
 * 좌표의 작품 정보. 코드워드만 보고 만든다. 렌더와 무관하다.
 *
 * lang 을 주지 않으면 지금 언어를 쓴다. 테스트는 명시해서 부른다.
 */
export function describe({ tier, x, y, code, lang = language() }) {
  // 로비에는 작품이 없다. 제목도 소장품 번호도 없다.
  // 부르는 쪽이 로비에서 이 함수를 부르지 않는 것이 옳지만, 조용히 이상한
  // 값을 내는 것보다 분명히 거절하는 편이 낫다.
  if (isLobbyTier(tier)) throw new RangeError('로비에는 작품이 없다');

  const spec = tierSpec(tier);
  const fields = decodeFields(spec, code);
  const { quant, baseLuma, baseCb, baseCr } = fields.header;

  const luma01 = baseLuma / 63;
  const primary = colorName(baseCb, baseCr, 7.5, luma01, lang);
  const secondary = colorName(fields.cb[0], fields.cr[0], 8, luma01, lang);

  // 제목의 조각들은 코드워드의 **서로 겹치지 않는** 자리에서 뽑는다.
  //
  //   31~36  형용사 (6비트, 64)
  //   43~48  명사   (6비트, 64)
  //   59~62  형식   (4비트, 16)
  //   67~75  명작 여부 (9비트, 512 중 0일 때만)
  //   76~79  명작 번호 (4비트, 16)
  //   81~83  연작 여부 (3비트, 8 중 0일 때만)
  //   84~86  연작 번호 (3비트, 8)
  //
  // 가장 얕은 층의 코드워드가 424비트이므로 87비트를 쓰는 데 여유가 크다.
  // 자리가 겹치면 두 조각이 함께 움직여 조합이 줄어든다. `test/label.test.mjs`
  // 가 조합의 다양성을 세어 그것을 잡는다.
  const pick = (shift, size) => Number((code >> BigInt(shift)) & BigInt(size - 1));
  const parts = {
    a: primary,
    b: primary === secondary ? null : secondary,
    adjective: tableFor(ADJECTIVES, lang)[pick(31, 64)],
    noun: tableFor(NOUNS, lang)[pick(43, 64)],
  };

  // 아주 드물게(1/512) 틀에서 나오지 않은 고유 제목이 붙는다.
  const masterwork = pick(67, 512) === 0;
  let title = masterwork
    ? tableFor(MASTERWORKS, lang)[pick(76, 16)]
    : tableFor(FORMS, lang)[pick(59, 16)](parts);

  // 연작 번호. 명작에는 붙이지 않는다 — 고유한 이름에 번호를 달면 고유하지 않다.
  if (!masterwork && pick(81, 8) === 0) title += ` ${SERIES[pick(84, 8)]}`;

  // 소장품 번호. 층과 좌표 축약만 쓴다. 국소성 단계는 관람객에게 뜻이 없다.
  const x36 = toBase36(x);
  const y36 = toBase36(y);
  const accession = `${tier}-${x36.slice(-3)}${y36.slice(-3)}`;

  // 전시실. 코드워드가 아니라 **좌표**에서 나온다.
  //
  // 그래서 이 값만은 code 로 계산할 수 없다. 같은 코드워드가 다른 자리에 있으면
  // 다른 방이고, 따라서 다른 그림이다. 전시실을 좌표에서 유도하기로 한 결과다.
  const roomIndex = roomOf(x, y);

  return {
    title,
    accession,
    quant,
    bytes: spec.byteLength,
    bits: spec.totalBits,
    zones: spec.blockCount,
    palette: { primary, secondary },
    room: { index: roomIndex, total: ROOMS.length, id: ROOMS[roomIndex].name },
  };
}
